export enum PetStatus {
  LOST = 'LOST',
  FOUND = 'FOUND',
  REUNITED = 'REUNITED',
  ACTIVE = 'ACTIVE'
}

export enum PetType {
  DOG = 'DOG',
  CAT = 'CAT',
  BIRD = 'BIRD',
  OTHER = 'OTHER'
}

export enum UserRole {
  USER = 'USER',
  ADMIN = 'ADMIN',
  MODERATOR = 'MODERATOR'
}

export enum NotificationType {
  PET_LOST = 'PET_LOST',
  PET_FOUND = 'PET_FOUND',
  PET_NEARBY = 'PET_NEARBY',
  MESSAGE = 'MESSAGE'
}

export enum ReportType {
  LOST = 'LOST',
  FOUND = 'FOUND',
  SIGHTING = 'SIGHTING'
}
